import { Layout } from '@components';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Layout>
      <div>Home Page</div>
    </Layout>
  );
};

export default Home;
